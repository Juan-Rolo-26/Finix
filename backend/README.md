# Backend (Finix)

Este folder ejecuta el backend real que vive en `apps/api`.

Comandos:
- `npm start` inicia la API (NestJS)
- `npm run dev` inicia la API en modo watch
- `npm run seed` carga datos de prueba
