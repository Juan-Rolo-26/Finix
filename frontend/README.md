# Frontend (Finix)

Este folder ejecuta el frontend real que vive en `apps/web`.

Comandos:
- `npm run dev` inicia Vite
- `npm run build` compila para producción
- `npm run preview` previsualiza el build
